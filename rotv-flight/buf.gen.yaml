version: v2
managed:
  enabled: true
plugins:
  # bufbuild/es v2 embeds service descriptors in _pb files — no separate _connect plugin needed
  - remote: buf.build/bufbuild/es:v2.2.3
    out: src/gen
    opt: [target=js+dts, import_extension=none]
inputs:
  - directory: proto
